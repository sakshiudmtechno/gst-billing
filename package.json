{
  "name": "udm-crm-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx tsx server.ts",
    "build": "npx esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "start": "node dist/server.cjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "dotenv": "^17.2.3",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tsx": "^4.23.13"
  }
}
